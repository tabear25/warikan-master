# Expo / React Native
node_modules/
.expo/
dist/
web-build/

# Native build output (generated by `expo prebuild`)
/android
/ios

# Env
.env
.env.local

# Metro / Expo caches
*.log
.expo-shared/

# Local Android build toolchain (JDK / Android SDK) — large, machine-local
.tooling/

# macOS
.DS_Store
